Function GetGameLabel(Data As String, StartString As String, EndString As String) As String
StartNum = InStr(7, Data, StartString)
EndNum = InStr(7, Data, EndString)
For i = StartNum To EndNum
    getGamelable = getGamelable & Mid(Data, i, 1)
Next
End Function


Private Sub Send_GameData_ConnectionRequest(ByVal requestID As Long)
'检查控件的 State 属性是否为关闭的。
'如果不是，
'在接受新的连接之前先关闭此连接。
If Server.State <> sckClosed Then _
Server.Close
'接受具有 requestID 参数的
'连接。
Server.Accept requestID
End Sub

Private Sub Send_GameData_DataArrival(ByVal bytesTotal As Long)
'为进入的数据声明一个变量。
Dim Data As String
Data = ""
Send_GameData.GetData Data
If Left(Data, 6) = "<Data>" Then
    Select Case Mid(Data, 7, GetGameLabel(Data, "[", "]"))
    Case "[Ammo]"
        AmmoData = Mid(AmmoData, InStr(1, AmmoData, "]") + 1)
        For i = 1 To Len(AmmoData)
            ammo(i) = CBool(Val(Mid(AmmoData, i, 1)))
        Next
    Case "[AmmoNum]"
    
    Case "[Blood]"
    
    Case "[Text]"
    
    Case "[Tool]"
    
    End Select
Else
    Information.Text = Information.Text & Data
End If
End Sub



Private Sub Command5_Click()
If Send_GameData.State = 7 Then Send_GameData.SendData "<Text>" & Text2.Text
End Sub




'*************************************************************************
'   标签         作用

'<Data>[Ammo]    数据 子弹

'<Data>[AmmoNum] 数据 子弹标

'<Data>[Blood]   数据 血量

'<Data>[Text]    数据 游戏信息

'<Data>[Tool]    数据 工具

'<Text>          玩家互相发送的信息

'*************************************************************************














'-------------------------------------------------------------------------------------------------------------------------